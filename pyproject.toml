[project]
name = "global-stress-dashboard"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["activfinancial"]

[tool.uv.sources]
activfinancial = { path = "vendor/activfinancial-1.11.2-py3-none-any.whl" }

[tool.setuptools]
packages = []

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
